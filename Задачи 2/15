def multiply(str):
    ns = [float(n.strip()) for n in str.split(',')]
    res = 1
    for n in ns:
        res *= n
    return res
str = "32, 3, 4.2, 12"
res = multiply(str)
print(f"Результат умножения чисел: {res}")
